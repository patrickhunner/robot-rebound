## New Features

## Submitted For Review (move new features here once completed)

1. **Add smooth movement and host speed control** — Robot position changes now slide between board cells. The host can change a shared speed from 1 (slowest) to 10 (fastest) during any phase; rooms default to 5 and reduced-motion preferences remain supported.
2. **Play optimal strategies for the room** — Each shortest strategy has a Play button. Playback validates and resets the shared review board, runs the moves at the host's selected speed for every player, updates the shared move count, and temporarily blocks conflicting review interactions.
